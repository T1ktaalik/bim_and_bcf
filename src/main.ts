import { createApp } from 'vue'

// Import CSS files in correct order (all @import rules must come first)
import './assets/css/main-consolidated.css'
import App from './App.vue'

createApp(App).mount('#app')
